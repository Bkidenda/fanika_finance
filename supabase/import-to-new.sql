-- Use this file as a template for the new project's SQL editor.
-- Replace each section with the rows exported from the old project.

-- Example pattern:
-- INSERT INTO public.profiles (id, full_name, email, currency, gross_income, created_at, updated_at)
-- VALUES
-- ('00000000-0000-0000-0000-000000000000', 'Example User', 'user@example.com', 'KES', 0, now(), now());

-- Add one INSERT block per table from the old project export.
